<!--
Keep the sections that apply and delete the rest. See CONTRIBUTING.md.
Link the issue so merging closes it: "Fixes #N".
-->

Fixes #

## Cause

What actually produced the behavior, with `file:line` references. Skip for new features.

## Change

What was done about it.

## Documentation

Which docs changed and why — `CHANGELOG.md` (required for changes under `wpf/` or `src/`), `README.md`
for user-facing behavior, `wpf/README.md` for implementation traps.

## Verification

State what was actually run and what was only reasoned about:

- [ ] `.\wpf\build.ps1` builds clean
- [ ] `--self-test` exits 0
- [ ] `--smoke-test` exits 0
- [ ] Behavior observed by hand (say how, and which build/version you ran)

Anything you could **not** verify belongs here explicitly, not left implied.
